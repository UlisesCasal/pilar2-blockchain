apiVersion: v1
kind: Service
metadata:
  name: {{ include "blockchain.fullname" . }}-coordinator-service
spec:
  selector:
    app: coordinator
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
